name: Publish Site

on:
  push:
    branches: [ master ]

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build masterful-minimalism source
        run: |
          wget https://github.com/maxstanley/masterful-minimalism/archive/refs/tags/v0.1.0.zip -O masterful-minimalism.zip
          ls -lah
          unzip masterful-minimalism.zip
          ls -lah
          (cd masterful-minimalism-0.1.0/; go build -v)
          cp masterful-minimalism-0.1.0/masterful-minimalism .

      - name: Create site using masterful-minimalism
        run: |
          ./masterful-minimalism -input=src/
          cp -r src/static/* output/
          echo "maxstanley.uk" > output/CNAME

      - name: Deploy to GitHub Pages
        if: success()
        uses: crazy-max/ghaction-github-pages@v2
        with:
          target_branch: gh-pages
          build_dir: output
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Purge Cloudflare Cache
        uses: jakejarvis/cloudflare-purge-action@master
        env:
          CLOUDFLARE_ZONE: ${{ secrets.CLOUDFLARE_ZONE }}
          CLOUDFLARE_TOKEN: ${{ secrets.CLOUDFLARE_TOKEN }}
